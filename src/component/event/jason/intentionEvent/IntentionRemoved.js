import React from "react";
import Event from "../../../Event";

function IntentionRemoved(props) {
    return (
        <div>
            <Event event={props.event}/>
        </div>
    )
}

export default IntentionRemoved;